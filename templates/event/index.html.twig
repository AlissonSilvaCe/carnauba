{% extends 'base.html.twig' %}

{% block title %}Carnaúba | Controle de Eventos{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Controle de Eventos</h1>
    <a href="#">Incluir</a>
    <table>
            <thead>
                    <tr>
                        <th>Início</th>
                        <th>Fim</th>
                        <th>Nome</th>
                        <th>Local</th>
                        <th colspan="2">Ações</th>
                    </tr>
            </thead>
            <tbody>
                {% for event in events %}
                    <tr>
                        <td>{{ events.initial_date}}</td>
                        <td>{{ events.final_date}}</td>
                        <td>{{ events.name}}</td>
                        <td>{{ events.local}}</td>
                        <td></td>
                        <td></td>
                    </tr>
                {% endfor %}
            </tbody>
    </table>

</div>
{% endblock %}